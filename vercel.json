{
    "buildCommand": "npm run vercel-build",
    "outputDirectory": "dist",
    "framework": null,
    "cleanUrls": true,
    "routes": [
      { "handle": "filesystem" },
  
      { "src": "/share/(.*)", "dest": "/api/share?handle=$1" },
      { "src": "/(.*)", "dest": "/" }
    ]
  }
  